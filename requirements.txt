slack-bolt==1.15.0        # Slack Bolt for Python
slack-sdk==3.19.4         # Slack SDK for Python
fastapi==0.95.2           # FastAPI framework
uvicorn==0.22.0           # ASGI server for FastAPI
python-dotenv==1.0.0      # .envファイルの環境変数を読み込む
aiohttp==3.8.1            # 非同期HTTPクライアント
google-generativeai>=0.3.0  # 検索システムで必要（Gemini APIなど）
httpx==0.27.0             # FastAPIのTestClient用（pytest実行時に必要）
pytest==8.3.4             # テストフレームワーク
pluggy==1.5.0             # pytest のプラグイン管理
